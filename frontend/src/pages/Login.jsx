import {useState} from 'react';import {useNavigate} from 'react-router-dom';import api from '../api';export default function Login(){const [username,setU]=useState(''),[password,setP]=useState(''),[msg,setM]=useState(''),nav=useNavigate();const submit=async e=>{e.preventDefault();try{const r=await api.post('/auth/login',{username,password});localStorage.setItem('token',r.data.token);nav('/dashboard')}catch(e){setM(e.response?.data?.message||'Login failed')}};return <section><h2>Admin Login</h2><form onSubmit={submit}><input placeholder="Username" value={username} onChange={e=>setU(e.target.value)} required/><input type="password" placeholder="Password" value={password} onChange={e=>setP(e.target.value)} required/><button>Login</button></form><p>{msg}</p></section>}
